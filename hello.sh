#!/bin/bash

# If you declare this header, it will use the shell referenced, so you won't need to use 
# sh ./hello.sh or bash ./hello.sh
# to run it, you can just directly call it instead:
# ./hello.sh

echo "Hello, friend!"
